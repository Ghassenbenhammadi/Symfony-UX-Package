{% extends 'base.html.twig' %}

{% block title %}DeailyResult{% endblock %}

{% block body %}
    <h1>DeailyResult</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deaily_result.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ deaily_result.date ? deaily_result.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ deaily_result.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_deaily_result_index') }}">back to list</a>

    <a href="{{ path('app_deaily_result_edit', {'id': deaily_result.id}) }}">edit</a>

    {{ include('deaily_result/_delete_form.html.twig') }}
{% endblock %}
